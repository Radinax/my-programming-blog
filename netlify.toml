# netlify.toml

[build]
  command = "npx playwright install chromium && npm run build"
  publish = "dist"

[build.environment]
  PLAYWRIGHT_BROWSERS_PATH = "0"
  # Astro 7 requires Node >= 22.12.0 (was "18", which failed the build).
  NODE_VERSION = "22"